Pod::Spec.new do |s|
  s.name     = 'TITokenField'
  s.version  = '1.0.1'
  s.source   = { :git => 'https://github.com/margarina/TITokenField.git' }
  s.source_files = 'TITokenField.{h,m}'
  s.requires_arc = true
end